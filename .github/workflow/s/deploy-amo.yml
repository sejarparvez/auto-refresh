name: Deploy to AMO

on:
  release:
    types: [published]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Build extension (production)
        run: bun run build:prod

      - name: Create extension zip (correct structure)
        run: |
          # Create zip with ALL files at root level
          # manifest.json, popup.html, dist/, icons/, fonts/ must be at zip root
          cd ${{ github.workspace }}
          
          # Verify files exist
          echo "=== Checking files ==="
          ls -la manifest.json popup.html
          ls dist/ icons/ fonts/
          
          # Create zip - files will be at root of zip
          # Using -j flag to junk paths (store only filenames)
          # Actually, we need to cd into each dir and add files
          
          # Better approach: create temp dir and copy files there
          mkdir -p /tmp/extension
          cp manifest.json /tmp/extension/
          cp popup.html /tmp/extension/
          cp -r dist /tmp/extension/
          cp -r icons /tmp/extension/
          cp -r fonts /tmp/extension/
          
          # Create zip from temp dir
          cd /tmp/extension
          zip -r ${{ github.workspace }}/auto-refresh-${{ github.event.release.tag_name }}.zip .
          
          # Verify zip structure
          echo "=== Zip contents (manifest.json should be at root) ==="
          cd ${{ github.workspace }}
          unzip -l auto-refresh-${{ github.event.release.tag_name }}.zip | head -20

      - name: Sign and upload to AMO
        if: ${{ secrets.AMO_API_KEY != '' && secrets.AMO_API_SECRET != '' }}
        env:
          AMO_API_KEY: ${{ secrets.AMO_API_KEY }}
          AMO_API_SECRET: ${{ secrets.AMO_API_SECRET }}
        run: |
          bunx web-ext sign \
            --source-file auto-refresh-${{ github.event.release.tag_name }}.zip \
            --artifacts-dir amo-artifacts \
            --api-key "$AMO_API_KEY" \
            --api-secret "$AMO_API_SECRET" \
            --channel listed

      - name: Manual upload reminder
        if: ${{ secrets.AMO_API_KEY == '' || secrets.AMO_API_SECRET == '' }}
        run: |
          echo "::warning::AMO_API_KEY or AMO_API_SECRET not set. Please upload manually to AMO."
          echo "1. Download the zip from the release assets"
          echo "2. Go to https://addons.mozilla.org/en-US/developers/addon/submit/"
          echo "3. Upload the zip file manually"
          echo "4. After approval, add your extension ID to manifest.json"
          echo "5. Add AMO_API_KEY and AMO_API_SECRET to GitHub secrets"
